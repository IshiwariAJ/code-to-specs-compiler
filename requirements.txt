# ===========================================================
# code-to-specs-compiler — 依存パッケージ
# ===========================================================

# ── コア（必須）────────────────────────────────────────────
# tree-sitter 本体は常に必要
tree-sitter>=0.21.0

# ── 言語パッケージ（使用する言語のみインストール可）────────
# 未インストールの言語は起動時に自動スキップされる。
# 1つ以上インストールすること。
tree-sitter-typescript>=0.21.0
tree-sitter-python>=0.21.0
tree-sitter-go>=0.21.0
tree-sitter-powershell>=0.26.0
tree-sitter-java>=0.21.0
