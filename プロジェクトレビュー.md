# 自然言語コンパイラ プロジェクトレビュー

レビュー日: 2026-05-28（前回 2026-05-27）

改善反映メモ: 前回レビュー後、Phase R6（条件分岐ボディのネストIR化）と Phase R7（try/catch/finally の IR 化、`TryCatchNode` 追加）が実装され、テストが 411件 → 422件に増加。`pytest --basetemp .pytest_tmp_review2` で 422 passed を確認しました（環境に `tree-sitter-powershell` を追加インストールする必要あり）。

---

## 総評

前回レビューで P0 / P1 に挙げた項目のうち、ドキュメントのスコープ修正（「100%」「完全等価」→ 「対応済み構文を決定論的に」）、未対応拡張子のエラー化、抽出警告の明示、条件分岐ボディのネストIR化は完了しています。さらに `try / catch / finally` が `TryCatchNode` として実装され、TypeScript / Python / Java / PowerShell の例外処理が構造化IRとして出力されるようになりました。Phase R6 / R7 はプロジェクトの「監査用途」価値を大きく押し上げています。

一方で、kitchen-sink 化された `examples/sample.ts` を実際にコンパイルして検証すると、トップレベルの未対応構文（TypeScript `class`、Python docstring が body 内で重複検出される現象、アロー関数の隠れた変数化）が、抽出警告に出ないまま静かにドロップ／誤分類されるケースが残っています。これは「未対応構文は仕様化されないが、警告として明示する」というプロジェクトの中核約束に対するリスクです。次のフェーズでは、機能追加よりもまず「未抽出のトップレベル要素を必ず警告に乗せる」整備を優先するのが効果的です。

---

## 良い点（前回からの進展を含む）

1. **try / catch / finally が IR 化された**
   - `TryCatchNode` が `try_body` / `catch_body` / `finally_body` を再帰IRとして保持し、Markdownでは「例外処理」セクションの下に try / catch / finally の各ブロックが構造化表示されます。
   - `examples/sample.ts` の try ブロックで、`configText` への代入・`console.log`・catch 内の `console.error` と再スロー・finally 内の `auditCounter += 1` がすべて構造化された IR として表示されることを確認しました。

2. **テスト件数が 411件 → 422件に増加し、全グリーン**
   - 新規 11件は `TryCatchNode` の mapper / renderer / pipeline テスト。
   - 内訳: `test_mapper.py` 193件 / `test_renderer.py` 105件 / `test_pipeline.py` 85件 / `test_batch.py` 39件。

3. **ドキュメントのスコープが正確になった**
   - README は「対応済み構文を決定論的に Markdown 仕様書へ変換するCLIツール」に修正済み。
   - 企画書も「決定論的（同一入力から同一出力）。対応済みIRについてはコード由来の情報のみを出力し、未対応構文は警告またはレポートで明示」に修正済み。
   - 「新言語追加は1ファイルだけ」も「既存の中核ファイルを変更せずに追加できます。通常は parser / languages / 依存パッケージ / テスト / サンプルを追加します」に修正済み。

4. **未対応拡張子のフォールバックが廃止された**
   - `select_plugin()` は未知の拡張子で `ValueError` を投げる仕様になっており、`compile_to_spec()` を API 的に使った場合も誤った前提でTypeScript解析されることがなくなりました（`src/pipeline.py:43`）。

5. **`examples/*` が kitchen sink 化された**
   - `sample.ts` / `.py` / `.go` / `.ps1` / `.java` は対応済み構文と未対応構文の両方を含むようになっており、出力末尾の抽出警告セクションで「いま何が拾えていないか」が見えるようになりました。これは監査用途に向いた良い設計判断です。

6. **アーキテクチャの不変条件が守られている**
   - `src/ir/mapper.py` には言語名による分岐がなく、`LanguageProfile` / `LanguagePlugin` を介して言語差異が局所化されています。
   - 新言語追加で `mapper.py` が変更不要、というR2 の設計目標が引き続き保たれています。

---

## 重要な指摘（今回の検証で見えた残課題）

### 1. トップレベルの未対応構文が抽出警告に出ないまま黙ってドロップされる ⚠️ P0

`examples/sample.ts` の `class AuditLog`（行32〜38）は次の流れで「無いことになる」現象が起きています。

該当箇所:
- `src/languages/typescript.py:43` — `class_node_types=frozenset()`
- `src/languages/typescript.py:269` — `class_extractor=lambda _: None`
- `src/ir/mapper.py:1301` — `if not plugin.profile.class_node_types: return ()`
- `src/ir/mapper.py:1087-1093` — `_format_unmapped_statement_warning` は関数本体内の文ノードのみ対象

トップレベル走査（`_extract_all_imports` / `_extract_all_module_variables` / `_extract_all_type_definitions` / `_extract_all_class_definitions`）はそれぞれ `profile.X_node_types` に該当しないノードを単純にスキップします。どのリストにも該当しなかったトップレベル宣言は、警告にもログにも出ません。

影響:
- TypeScript の `class` 宣言、TypeScript / Python の enum / Go の `struct` / `type` 宣言、トップレベルの式文など、「拾えなかったトップレベル要素」がレビュアーから不可視のまま仕様書が完成する。
- 「未対応構文は警告として明示する」というプロジェクトの中核約束が、関数の中だけで成立して、関数の外では成立していない。

推奨:
- `map_source_to_module_spec()` 内で、トップレベルノードのうちどの抽出器にもマッチしなかったものを `extraction_warnings` に追加する。少なくとも `class_declaration` / `enum_declaration` / 未対応の `expression_statement` を「未対応のトップレベル宣言」として警告化する。
- もしくは、抽出警告とは別に「未抽出のトップレベル要素一覧」セクションを必ず出す（無ければ「未抽出のトップレベル要素はありません」と明示）。

### 2. TypeScript アロー関数が「定数」として静かに変数テーブルに入る ⚠️ P0

`examples/sample.ts:123` の `const arrowBenefit = (value: number): number => (value > 0 ? value : 0);` は `extract_ts_module_variable()` を経由して「`arrowBenefit` という定数で、値テキストが `(value: number): number => (value > 0 ? value : 0)`」として `モジュール定数・変数` セクションに表示されます。

該当箇所:
- `src/languages/typescript.py:105-137` — `arrow_function` を含む `variable_declarator` も `ModuleVariableSpec` に変換される
- 警告は出ない（モジュール変数として「対応した」扱いになるため）

影響:
- 仕様書の読み手は `arrowBenefit` を「単なる定数」と認識する。実体は関数で、引数・戻り値・本体ロジックを持つが、すべて1行の生テキストに圧縮される。
- AI 生成コードのレビュー前処理という訴求にとっては、ここはむしろ前提条件として「関数」を表現できないのは痛い箇所です。

推奨:
- 短期: `variable_declarator` の value が `arrow_function` の場合は `ModuleVariableSpec` を生成せず、`extraction_warnings` に「アロー関数の本体は仕様化対象外です」と明示する。
- 中期: `arrow_function` を `FunctionSpec` に変換する経路を追加する（IMPROVEMENT_PLAN ⑤ と同方向）。

### 3. Python の docstring が「未対応の expression_statement」として警告に二重計上される ⚠️ P0

`examples/sample.py` の出力末尾に次の警告が出ます:

```
- 関数 `calculate_user_benefit`: expression_statement は未対応のため仕様化されませんでした: `"""Supported-heavy function: ..."""`
- 関数 `inspect_unsupported_flow`: expression_statement は未対応のため仕様化されませんでした: `"""Unsupported-heavy function: ..."""`
```

該当箇所:
- `src/languages/python.py:51-70` — `extract_py_function_docstring` は body 先頭の docstring を `description` として正しく抽出
- `src/ir/mapper.py:885-905` — `_map_expression_statement_to_ir` は string リテラル単独の `expression_statement` を IR 化しないため None を返す
- `src/ir/mapper.py:1120-1121` — None が返ったため `extraction_warnings` に追加されてしまう

影響:
- docstring は関数の説明として既に「対応済み」として扱われているのに、警告にも「未対応」として表示される false positive。
- 「警告に出てくる項目は本当に拾えていない構文だけ」という前提が崩れているため、警告の信頼性が下がる。

推奨:
- `_extract_body_ir_nodes()` の中で、body 先頭の `expression_statement` が文字列リテラル単独で、かつ profile が `has_module_docstring`（または関数 docstring を持つ言語フラグ）の場合はスキップする。
- もしくは、`_map_statement_to_ir` 内で docstring 用の専用判定（先頭 string リテラル）を追加して `None` ではなく「docstring 取得済み」のセンチネルを返し、警告から除外する。

### 4. 分割代入が DataTransformation として低忠実度のまま表示される（P1）

`examples/sample.ts` の `const [first = 0, ...rest] = values;` は次のように出力されます:

```markdown
* `[first = 0, ...rest]` に代入する: `values`
```

該当箇所:
- `src/ir/mapper.py:908-934` — `_map_lexical_declaration_to_ir` が左辺のパターンをそのまま `target` に詰める

影響:
- 仕様書の読み手は `[first = 0, ...rest]` というテキストを「変数名」と読みかねない。
- 「同一入力から同一出力」は保たれているが、可読性は分割代入で大きく落ちる。

推奨（IMPROVEMENT_PLAN ⑬ と同方向）:
- `array_pattern` / `object_pattern` を検出して、`DataTransformation` ではなく分割代入専用の IR（`DestructureAssignment` 等）に分ける。
- 当面の暫定処理として、`[`/`{`を含む `target` を検出したら警告に「分割代入は構造化表現未対応」と添える。

### 5. ループの抽出警告に余計な情報が出る（軽微）

`examples/sample.java` の出力で `for_statement は未対応のため仕様化されませんでした` が出ます。Java は `enhanced_for_statement`（for-each）のみ対応で、C スタイルの `for` は未対応のため正しい警告ですが、これは IMPROVEMENT_PLAN ④（while ループ追加）と同じカテゴリで Java の `for (int i = 0; ...)` も合わせて対応するとよさそうです。

---

## 優先度付き改善提案（更新版）

### P0: 信頼性に直結（中核約束を守るために必要）

1. **トップレベルの未対応構文を必ず警告化する**。`class_declaration`、`enum_declaration`、Go の `type_declaration`、PowerShell の `class` 等が現状は静かに消える。
2. **TypeScript アロー関数を「定数」として変数テーブルに入れない**（最低限警告化）。
3. **Python の関数 body 内 docstring を抽出警告から除外する**（false positive）。

### P1: 実用上の表現力を上げる（IMPROVEMENT_PLAN の残り）

1. アロー関数を `FunctionSpec` として抽出する（IMPROVEMENT_PLAN ⑤）。
2. `while` / `do-while` を `LoopNode` の `WHILE` 種別として追加する（同 ④）。
3. `switch` / Python `match` を IR 化する（同 ⑧）。
4. `async / await` マーカーを `FunctionSpec` / `SideEffect` に追加する（同 ⑨）。
5. `break` / `continue` を `FlowControlNode` として追加する（同 ⑫）。

### P2: BtoB 導入に近づける

1. 抽出カバレッジレポート（対応済みノード数 / 未抽出ノード数 / カバー率）を仕様書末尾に追加する。
2. Git hook / CI で仕様書の自動再生成を提供する。
3. PR 差分の仕様書 diff をレビューに乗せるサンプル CI 設定を `docs/` に追加する。
4. `examples/sample.*` の対応／未対応の対応表を README に書き、何が拾える／拾えないかを一目で確認できるようにする。

---

## アーキテクチャに関する追記

`src/ir/mapper.py` は引き続き言語名参照ゼロを維持しています。新たに追加された `TryCatchNode` の処理（`_map_try_to_ir`）も、`profile` と `plugin` 経由でフィールド名（`body` / `finalizer` / `catch_clauses` / `finally_clause` 等）を抽象化しており、TS / Python / Java / PowerShell の AST 差異を一箇所に閉じ込められています。これは R2 の設計目標を保ったまま機能拡張できているという意味で、素直で堅い実装です。

懸念は1点だけ:
- `_extract_body_ir_nodes` の警告システムが「関数本体」内に限定されており、トップレベル走査側に同等の警告生成パスがありません。`_format_unmapped_statement_warning` をトップレベル走査側にも呼び出す薄いラッパーを追加すれば、P0 の指摘（1）はかなり簡潔に解消できます。

---

## 検証結果

実行コマンド:

```powershell
# tree-sitter-powershell を未インストール状態だと PowerShell 関連の 14 テストが失敗する
pip install tree-sitter-powershell
pytest --basetemp .pytest_tmp_review2
```

結果:

- 通常 `pytest`: Windows の既存 `.pytest_tmp/` 権限エラーで `tmp_path` フィクスチャが失敗するケースが残存（37 errors）。
- `pytest --basetemp .pytest_tmp_review2`: **422 passed**（1.17s）。

サンプル出力の手動確認:

```powershell
python main.py examples/sample.ts
python main.py examples/sample.py
python main.py examples/sample.java
```

検出:
- `examples/sample.ts:32` の `class AuditLog` が出力に存在しない（警告にも出ない）。
- `examples/sample.ts:123` のアロー関数 `arrowBenefit` が「定数」として変数テーブルに表示される。
- `examples/sample.py` の docstring が「未対応の `expression_statement`」として警告に二重計上される。

---

## 結論

前回レビュー時点と比べ、try/catch/finally の IR 化（Phase R7）、条件分岐ボディのネスト解析（Phase R6）、ドキュメントのスコープ修正、未対応拡張子のエラー化が完了しており、「監査向け決定論的コンパイラ」というポジショニングはほぼ実体を伴うようになっています。

次の差別化は機能追加よりも、**「拾えなかった要素を必ず警告に乗せる」抽出カバレッジ整備の徹底**にあります。これは大きな工数を必要としませんが、BtoB / 監査用途では「100%カバーしている」ことより「カバーしていないものを漏れなく明示する」ことのほうが価値が高いため、ここを固めると訴求がより強固になります。

具体的には次の3点を P0 として推奨します:
1. トップレベルの未対応宣言（TS class、Go struct 等）を抽出警告に追加。
2. アロー関数を変数として静かに取り込まない（警告化）。
3. Python 関数 body 内の docstring を警告から除外する。

これらを入れた上で、IMPROVEMENT_PLAN 残項目（while / アロー関数の FunctionSpec 化 / switch / async / break / 分割代入）を優先度順に進めていく形がよさそうです。
