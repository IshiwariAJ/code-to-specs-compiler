# 自然言語コンパイラ プロジェクトレビュー

レビュー日: 2026-05-27

改善反映メモ: 本レビュー後、P0項目の一部（未対応拡張子のエラー化、未抽出構文の抽出警告、README/企画書のスコープ付き表現への更新）を実装済み。現在のテスト結果は `pytest --basetemp .pytest_tmp_verify4` で 408 passed。

## 総評

このプロジェクトは、AI生成コード時代の「読む・監査する」負荷に対して、LLMではなくAST解析とIRで対抗するという着眼点が強いです。実装もすでにPoCを超えており、TypeScript / Python / Go / PowerShell / Javaを同一Markdownフォーマットへ落とす3層構成、言語プラグイン、408件のテストが揃っています。

一方で、企画書・READMEで掲げている「100%正確」「コードと完全等価」「完全可能」という表現は、現状の実装範囲に対してかなり強すぎます。現段階では「決定論的に、対応済み構文を構造化仕様へ変換するツール」と表現するのが正確です。プロダクト価値は十分ありますが、監査・BtoB向けに出すなら、未対応構文を明示しないまま“完全性”を売るのはリスクになります。

## 良い点

1. **問題設定が明確**
   - 生成AIでコード作成速度が上がる一方、人間のレビュー負荷が上がるという課題設定は説得力があります。
   - LLM解説ではなく、ローカル・決定論・再現性を価値に置く方針は、企業利用や監査用途と相性がよいです。

2. **アーキテクチャが素直で拡張しやすい**
   - `Parser -> Universal IR -> Markdown Renderer` の分離が明確です。
   - `src/languages/` の `LanguagePlugin` に言語差分を寄せており、中心の `mapper.py` が言語名に依存しにくい構造になっています。
   - IR型が frozen dataclass で揃っていて、テストしやすい設計です。

3. **テスト資産が強い**
   - `pytest --basetemp .pytest_tmp_verify4` で 408件すべて成功しました。
   - mapper / renderer / pipeline / batch が分かれていて、層ごとの退行を検出しやすいです。

4. **「嘘をつかない」価値は成立している**
   - LLMではないため、コードに存在しない意図を勝手に補うリスクは低いです。
   - これはREADMEや企画書で前面に出してよい強みです。

## 重要な指摘

### 1. 「100%正確」「完全等価」は現状だと過大表現

企画書では、ソースコードから「100%正確な構造化自然言語」を生成し、「コードと完全等価」と位置づけています。

該当箇所:
- `次世代自然言語コンパイラ 開発プロジェクト 企画提案書.md:5`
- `次世代自然言語コンパイラ 開発プロジェクト 企画提案書.md:28`
- `次世代自然言語コンパイラ 開発プロジェクト 企画提案書.md:59`

しかし、現状では `try/catch/finally`、`while`、TypeScriptアロー関数、switch/match、async/await、break/continue、分割代入などが未対応または改善計画に残っています。

該当箇所:
- `IMPROVEMENT_PLAN.md:68`
- `IMPROVEMENT_PLAN.md:110`
- `IMPROVEMENT_PLAN.md:143`
- `IMPROVEMENT_PLAN.md:192`
- `PROJECT_STATUS.md:543`

推奨:
- 「100%正確」は「対応済み構文について、同一入力から同一出力を返す決定論的変換」と定義し直す。
- READMEと企画書に「対応構文マトリクス」と「未対応構文は出力しない/警告する」を明記する。
- 監査用途では、仕様書本体とは別に「抽出カバレッジレポート」を出すと信頼性が上がります。

### 2. 条件分岐の中身がIR化されず、生コード表示に留まっている

`CaseNode.action_texts` は `tuple[str, ...]` で、if/else各ケース内の処理が構造化IRではなく生テキストです。レンダラー側でも先頭行に丸めて表示するため、複数行の処理・ネストした処理・副作用・returnの意味が仕様書上で弱くなります。

該当箇所:
- `src/ir/types.py:158`
- `src/ir/types.py:163`
- `src/ir/mapper.py:303`
- `src/renderer/markdown.py:430`
- `IMPROVEMENT_PLAN.md:192`

影響:
- 「条件分岐の中で何が起きるか」を日本語仕様として監査しにくい。
- 複雑なビジネスロジックほど価値が出るはずのツールなのに、そこで表現力が落ちます。

推奨:
- `CaseNode.action_texts` を `body: tuple[IRNode, ...]` に変更する。
- 既存の `_extract_body_ir_nodes()` を条件分岐にも再利用する。
- これは大きめの変更ですが、プロダクト価値に直結するため優先度は高いです。

### 3. 未対応拡張子の扱いが危険

`select_plugin()` は未知の拡張子に対して TypeScript プラグインへフォールバックします。

該当箇所:
- `src/pipeline.py:25`
- `src/pipeline.py:33`
- `src/pipeline.py:44`
- `main.py:45`
- `main.py:69`

CLIでは警告が出ますが、その後もTypeScriptとしてコンパイルされます。`compile_to_spec()` をAPI的に使った場合も、未知の拡張子がTypeScript扱いになります。

影響:
- 「決定論的だが間違った前提で解析した仕様書」が生成される可能性があります。
- 監査用途では、これはハルシネーションとは別種の危険な誤出力になります。

推奨:
- 未対応拡張子は原則エラーにする。
- どうしてもフォールバックを残すなら、出力Markdownに「未対応拡張子をTypeScriptとして解析した」旨を明記する。
- `discover_plugins()` で `.ts` が登録されなかった場合のエラーも明示的に扱う。

### 4. 「新言語追加は1ファイルだけ」は言い切りすぎ

READMEでは「新言語の追加は `src/languages/<言語名>.py` を1ファイル作成するだけ」と説明していますが、実際には少なくとも parser ファイルと依存パッケージ、テスト、サンプルが必要です。

該当箇所:
- `README.md:41`
- `PROJECT_STATUS.md:338`
- `PROJECT_STATUS.md:492`
- `PROJECT_STATUS.md:495`

推奨:
- 表現を「既存の中核ファイルを変更せずに追加できる」に変える。
- 新言語追加の最小セットを `parser`, `languages`, `requirements`, `tests`, `examples` として明文化する。

## 優先度付き改善提案

### P0: 仕様上の信頼性を守る

1. 未対応拡張子をエラーにする。
2. 出力に「対応済み構文」と「未抽出ノード数/種類」を含める。
3. README/企画書の「100%」「完全等価」をスコープ付き表現に修正する。

### P1: 実用上の表現力を上げる

1. 条件分岐ボディをIR化する。
2. `try/catch/finally` と `while` を追加する。
3. TypeScriptアロー関数に対応する。
4. `switch` / Python `match` を追加する。

### P2: BtoB導入に近づける

1. Git hook / CI で仕様書の自動再生成を提供する。
2. 仕様書差分をPRレビューで見られるようにする。
3. VS Code拡張より先に、CIで使えるCLI品質を固める。
4. 「抽出カバレッジ」「未対応構文」「パースエラー」を一覧化した監査サマリを出す。

## 企画書への修正文案

現在:

> ソースコードから100%正確な構造化自然言語（仕様書）を自動生成する

推奨:

> ソースコードのASTから、対応済み構文を決定論的に抽出し、再現性のある構造化仕様書を自動生成する。LLMを使用しないため、コードに存在しない説明を付加しない。

現在:

> 100%決定論的（コードと完全等価）

推奨:

> 決定論的（同一入力から同一出力）。対応済みIRについてはコード由来の情報のみを出力し、未対応構文は警告またはカバレッジレポートで明示する。

## 市場・プロダクト観点

狙う市場は「学習ツール」よりも「レビュー補助」「レガシー解析」「監査証跡」のほうが合っています。ただし監査用途では、完全な仕様生成より先に「このツールが何を読めて、何を読めなかったか」を正直に出すことが価値になります。

推奨する最初の訴求:

- AI生成コードのレビュー前処理
- レガシーコードの構造棚卸し
- PR差分の仕様書化
- 多言語プロジェクトの処理フロー可視化

避けたい訴求:

- コードと完全等価な自然言語仕様
- あらゆる言語・あらゆる構文に対応
- 人間レビューを不要にする

## 検証結果

実行コマンド:

```powershell
pytest
pytest --basetemp .pytest_tmp_verify4
```

結果:

- 通常の `pytest`: 369 passed / 37 errors
- エラー原因: Windows上の既存一時ディレクトリへのアクセス拒否で、`tmp_path` フィクスチャ作成に失敗
- `pytest --basetemp .pytest_tmp_verify4`: 408 passed

補足:

- `.pytest_tmp/` は過去の検証で作成されましたが、削除時にWindows権限で拒否されました。
- `.gitignore` に `.pytest_tmp*/` を追加済みなので、Git管理対象にはなりません。

## 結論

プロジェクトの核はかなり良いです。特に「LLMに説明させる」のではなく「ASTから決定論的に仕様化する」という方向性は、AI時代の開発現場に対して差別化があります。

次の勝ち筋は、対応言語をさらに増やすことよりも、まず「未対応構文を黙って落とさない」「条件分岐内部まで構造化する」「100%という表現をスコープ付きにする」ことです。ここを固めると、BtoB向けの信頼できるレビュー補助ツールとしてかなり見え方が良くなります。
